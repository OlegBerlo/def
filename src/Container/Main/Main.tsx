import { Container } from "@mui/material";

type Props = {};
const Main = (props: Props) => {
  return (
    <Container
      sx={{
        padding: "40px 0",
      }}
    >
      Main
    </Container>
  );
};
export default Main;
